{
    "name": "ai-hiring-platform-root",
    "version": "1.0.0",
    "description": "Root project for AI Powered Agentic Hiring Platform",
    "scripts": {
        "start": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
        "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
        "dev": "npm start"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}